# core
numpy==2.4.0
pandas==2.3.3
scikit-learn==1.8.0
joblib==1.5.3

# model training
lightgbm==4.6.0
mlflow==3.8.0

# data download
kaggle==1.8.2
requests==2.32.5
python-dotenv==1.2.1

# api
fastapi==0.127.0
uvicorn==0.40.0
pydantic==2.12.5

# tests (so CI can run)
pytest==9.0.2
pytest-cov==7.0.0
coverage==7.13.0
httpx==0.28.1

# Windows-only helper (optional; if you still need it locally)
pywin32==311; platform_system == "Windows"
